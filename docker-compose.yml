services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped
